// Preload script for ReadSync desktop app.
// contextIsolation is enforced in BrowserWindow config.
// No contextBridge exposure is needed for the pure wrapper.
// Add IPC bridge here if future native API access is required.
