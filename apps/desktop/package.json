{
	"name": "desktop",
	"version": "0.0.2",
	"private": true,
	"main": "dist/main.js",
	"scripts": {
		"dev": "tsc && electron .",
		"build": "tsc && electron-builder",
		"package": "tsc && electron-builder"
	},
	"devDependencies": {
		"electron": "40.6.0",
		"electron-builder": "26.8.1",
		"typescript": "catalog:"
	}
}
